<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Aircraft Information</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f4f4f4;
        }

        h1 {
            text-align: center;
            margin-bottom: 20px;
        }

        ul {
            list-style-type: none;
            padding: 0;
            margin-top: 20px;
        }

        li {
            margin-bottom: 10px;
            padding: 10px;
            background-color: #fff;
            border-radius: 5px;
        }

        li:nth-child(even) {
            background-color: #f9f9f9;
        }

        strong {
            margin-right: 5px;
        }

        form {
            margin-bottom: 20px;
            text-align: center;
        }

        input[type="text"] {
            padding: 5px;
            margin-right: 10px;
        }

        button {
            padding: 5px 10px;
            background-color: #007bff;
            color: white;
            border: none;
            cursor: pointer;
        }

        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
<h1>Aircraft Information</h1>

<form action="/aircraft" method="GET">
    <label for="manufacturer">Manufacturer</label><input type="text" id="manufacturer" name="manufacturer">
    <label for="model">Model</label><input type="text" id="model" name="model" >
    <button type="submit">Search</button>
</form>

<ul>
    <% aircrafts.forEach(function(aircraft) { %>
        <li>
            <strong>Manufacturer:</strong> <%= aircraft.manufacturer %><br>
            <strong>Model:</strong> <%= aircraft.model %><br>
            <strong>Engine Type:</strong> <%= aircraft.engine_type %><br>
            <strong>Engine Thrust (lb-ft):</strong> <%= aircraft.engine_thrust_lb_ft %><br>
            <strong>Max Speed (knots):</strong> <%= aircraft.max_speed_knots %><br>
            <strong>Cruise Speed (knots):</strong> <%= aircraft.cruise_speed_knots %><br>
            <strong>Ceiling (ft):</strong> <%= aircraft.ceiling_ft %><br>
            <strong>Takeoff Ground Run (ft):</strong> <%= aircraft.takeoff_ground_run_ft %><br>
            <strong>Landing Ground Roll (ft):</strong> <%= aircraft.landing_ground_roll_ft %><br>
            <strong>Gross Weight (lbs):</strong> <%= aircraft.gross_weight_lbs %><br>
            <strong>Empty Weight (lbs):</strong> <%= aircraft.empty_weight_lbs %><br>
            <strong>Length (ft):</strong> <%= aircraft.length_ft %><br>
            <strong>Height (ft):</strong> <%= aircraft.height_ft %><br>
            <strong>Wing Span (ft):</strong> <%= aircraft.wing_span_ft %><br>
            <strong>Range (nautical miles):</strong> <%= aircraft.range_nautical_miles %><br>
        </li>
    <% }); %>
</ul>
</body>
</html>
